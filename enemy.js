class Enemy{
    constructor(x,y){
  x = this.positionX + random(10,300)
  y = this.positionY + random(10,140)
  this.height = 32
  this.width = 32
    }

    drawSprites(){
      drawSprites()
    }
}